function add(a:number,b?:number,c:number=0)
{
    return a + b + c
}

console.log(add(1,2))

//as the b parameter is optional
// if you pass 2 values a & b will execute
// if you pass 3 b takes a value as optional